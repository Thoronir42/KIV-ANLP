Before operation, add data/train.m, data/test.txt and data/morph.dict data files

Run with Python 3.7

Unit test can be lauched from current directory by command:
    python -m unittest discover -s src

The program itself should be launched from current directory by command:
    python src/program.py
